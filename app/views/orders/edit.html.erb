<h1><%= t('.edit_order') %></h1>
<div class="depot_form form-horizontal well">
	<fieldset>
		<legend>
			<%= t('.legend') %>
		</legend>
		<%= render 'form' %>
	</fieldset>
</div>

<%= link_to 'Show', @order %> |
<%= link_to 'Back', orders_path %>
